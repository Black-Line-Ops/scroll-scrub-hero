# Run artefacts — these are per-project and often large or paid-for
.scrub-hero/
keyframes/
segments/
storyboard.json
*.mp4
# ...except the README demo. A trailing comment on a pattern line is NOT a comment in
# gitignore, it becomes part of the pattern, so this must sit on its own line.
!docs/examples-reel.mp4
*_state.json

# Never commit a key
.env
.env.*

node_modules/
.DS_Store
Thumbs.db
